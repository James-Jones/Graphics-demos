
---NaCL template---
How to build:
  cd into the NaCL\template directory
  make PATH-TO-SDK-PEPPER-DIR (e.g make e:\nacl_sdk\pepper_18)
How to run
  enable native client in chrome from the chrome://flags/ page if you have not already done so
  start a server by running the provided httpd.py script
  browse to the port which the script prints out on the command line (e.g http://localhost:5103)

---Windows D3D demos---
In order to build these the SlimDX SDK is required. The SlimDX SDK and runtime are available at http://slimdx.org/download.php.
The visual studio project files are for the 2008 express version.
